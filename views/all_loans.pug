include partials/_head
include partials/_nav

h1 Loans
p
  a.button(href='new_loan.html') Create New Loan
p
  | Filters
  a(href='/loans')  All
  |  |
  a(href='/loans/overdue')  Overdue
  |  |
  a(href='/loans/checked_out')  Checked Out
table
  thead
    tr
      th Book
      th Patron
      th Loaned on
      th Return by
      th Returned on
      th Action
  tbody
    each loan in loans
      tr
        td
          a(href="FIX THIS") #{loan.book_id}
        td
          a(href="FIX THIS") #{loan.patron_id}
        td #{loan.loaned_on}
        td #{loan.return_by}
        td #{loan.returned_on}
        td
          a.button(href='return_book.html') Return Book
